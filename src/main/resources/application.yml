spring:
  security:
    user:
      name: user
      password: user

  datasource:
    url: jdbc:mysql://localhost:3306/askmeanything?serverTimezone=UTC&useLegacyDatetimeCode=false
    username: root
    password: admin
  jpa:
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.MySQLDialect
  sql:
    init:
      enabled: true